const express = require('express'); 
const app = express();
// const TripRouter=require('./routes/TripRouter'); //

app.use(express.json());
// app.usee('/api/v1/trips', TripRouter);
// if the request starts with /api/v1/trips,
// it will be handled by TripRouter
module. exports=
  {
    app
  };
